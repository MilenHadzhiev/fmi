package commands.commands;

import Items.Water;

public class DrinkWaterCommand extends LogItemCommand {
    public DrinkWaterCommand(Water item, String filePath) {
        super(item, filePath);
    }
}
